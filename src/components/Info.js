import * as React from "react";
import { styled } from "@mui/material";
import Grid from "@mui/material/Grid2";

const Info = () => {
  return <Grid container spacing={3} sx={{ margin: "3rem 0 0 0" }}></Grid>;
};

export default Info;
